# Database configuration
DATABASE_URL="postgres://user:password@localhost:5432/dbname?sslmode=disable"

# Port configuration
# The base port for the first lambda. Each subsequent lambda will use BASE_PORT + 1, +2, etc.
BASE_PORT=8080 